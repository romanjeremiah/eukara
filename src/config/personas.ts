// Compatibility export for existing imports. The implementation lives in the
// two-part persona directory: immutable core plus conditional clinical layer.
export {
	NAME,
	BASE_INSTRUCTION,
	FORMATTING_RULES,
	MENTAL_HEALTH_DIRECTIVE,
} from './persona';
